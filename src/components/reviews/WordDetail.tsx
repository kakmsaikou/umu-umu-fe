import { defineComponent } from 'vue';
import s from './WordDetail.module.scss';

export const WordDetail = defineComponent({
  props: {
    
  },
  setup: (props, context) => {
    return () => <div>WordDetail</div>;
  },
});